# TODO

* add `@apiVersion`
* trip name unique by user only
* trip geo queries
* public/private trips
* api include
* administration tasks (admin role, wipe database)
* Update dependencies
* Move to ES modules
